TODO:

Make it so dag, gimme, and peek all use the same subnet parsing as go
Make it so .bash_profile isn't needed to find $JSHOR (some require thing?)
Make all scripts get config
Make it so we don't have to source .bash_profile for JSHOR
Write help texts

Change this all to ruby?
