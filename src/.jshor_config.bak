
# globals

# user specific

ssh_key_gw='/Users/{{your_user}}/.ssh/'
ssh_key_cloud='/Users/{{your_user}}/.ssh/'
ssh_key_comp='/Users/{{your_user}}/.ssh/'
ssh_keys='/Users/{{your_user}}/.ssh/'

default_subnet='10.38.0'            # Oakland Altair Office Subnet

# used in scripts to pass to findSubnets.sh

snIndex='1'                         # skip localhost
iface=''                            # scan all ifaces

# used in go.sh, peek.sh, gimme.sh, and dag.sh

user='root'
dest="."

dag_pages=( 'Zuul/native' 'Zuul/zuulServer?cmd=get_status' 'Zuul/zuulServer?cmd=get_labels' 'Zuul/zuulServer?cmd=discovery')

# aliasAdder

al_iface="en0"                      # Your primary wifi interface
al_ip=""                            # Your most commonly used alias
al_nm="255.255.255.0"               # Standard netmask

echo "this should not be printed!!" # test for the sanitization

# JIRA settings

proj="CANDI"                        # Default JIRA project
browser="Google Chrome"              # Your favorite browser
at_server="candicontrols"
