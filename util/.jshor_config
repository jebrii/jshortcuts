
# globals

# user specific

ssh_key_gw='/Users/hraschke/.ssh/keys/candi4k'
ssh_key_cloud='/Users/hraschke/.ssh/keys/altair_rsa_key'
ssh_key_comp='/Users/hraschke/.ssh/keys/id_rsa'
ssh_keys='/Users/hraschke/.ssh/keys'

default_subnet='10.38.0'

# used in scripts to pass to findSubnets.sh

snIndex='1' # skip localhost
iface='' # scan all ifaces

# used in go.sh, peek.sh, gimme.sh, and dag.sh

user='root'
dest="."

dag_pages=( 'Zuul/native' 'Zuul/zuulServer?cmd=get_status' 'Zuul/zuulServer?cmd=get_labels' 'Zuul/zuulServer?cmd=discovery' )

# aliasAdder

al_iface="en0"          # Henry's wifi interface
al_ip="10.37.37.248"    # Preset IP for Henry on 10.37.37.0 subnet
al_nm="255.255.255.0"   # Standard netmask

echo "this should not be read"

# JIRA settings

proj="CANDI"
browser="Google Chrome"
jira_server="jira.prog.altair.com"
conf_server="confluence.prog.altair.com"
